server:
  port: 8888

spring:
  application:
    name: ad-dashboard

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server1:8001/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
